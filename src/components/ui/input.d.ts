// Type shim for the untyped input.jsx implementation (loose props by design;
// the .jsx file remains the runtime module — do not convert it).
import * as React from 'react';

export declare const Input: React.ComponentType<any>;
